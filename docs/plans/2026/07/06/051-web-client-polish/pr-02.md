# PR 051-2: Auction House Filters, Sorting, Pagination

> **Branch:** `feat/auction-house-polish`
> **Target:** Filters (category, price range), sorting (price, time ending), pagination

## Tasks

### 1. [coding] Extend auction API with filter/sort params
**File:** `apps/web/src/lib/auction.ts`

- Add `AuctionFilters` interface:
  ```typescript
  export interface AuctionFilters {
    query: string;
    category?: string;
    minPrice?: number;
    maxPrice?: number;
    sortBy?: 'price_asc' | 'price_desc' | 'ending_soon' | 'quantity';
    page?: number;
    perPage?: number;
  }
  ```
- Update `fetchListings` to accept `AuctionFilters`
- Build URL params from filters
- Add `ListingsResponse` schema with pagination metadata

### 2. [coding] Update useListings hook
**File:** `apps/web/src/queries/useAuction.ts`

- Change `useListings(query: () => string)` to `useListings(filters: () => AuctionFilters)`
- Update queryKey to include all filter params
- Keep `staleTime: 15_000` for cache

### 3. [coding] Create FilterBar component
**File:** `apps/web/src/components/FilterBar.tsx` (new, ≤150 LOC)

- Category dropdown (Weapon, Armor, Consumable, Material, All)
- Price range inputs (min/max gold)
- Sort dropdown (Price Low/High, Ending Soon, Quantity)
- Reset filters button
- All labels via `t()`

### 4. [coding] Create Pagination component
**File:** `packages/ui/src/Pagination.tsx` (new, ≤120 LOC)

- Shared UI component for pagination
- Props: currentPage, totalPages, onPageChange
- Previous/Next buttons, page numbers
- Accessible (aria-labels on buttons)
- Uses semantic tokens

### 5. [coding] Update AuctionHouse page
**File:** `apps/web/src/routes/AuctionHouse.tsx`

- Add state for filters (create `AuctionFilters` signal)
- Add state for pagination (page signal)
- Render `<FilterBar>` above table
- Render `<Pagination>` below table
- Update columns to show category badge

### 6. [coding] Add auction filter tests
**File:** `apps/web/src/components/FilterBar.test.tsx` (new)

- Test category dropdown changes filter
- Test price range inputs
- Test sort dropdown
- Test reset button clears all filters

### 7. [coding] Add pagination tests
**File:** `packages/ui/src/Pagination.test.tsx` (new, within ui package)

- Test Previous/Next buttons call onPageChange
- Test page numbers render correctly
- Test disabled states at edges

### 8. [coding] Add E2E test for filters
**File:** `apps/web/e2e/auction-filters.e2e.ts` (new)

- Test: select category → listings update
- Test: set price range → listings filter
- Test: change sort → order changes
- Test: pagination → navigate pages

## Success Criteria
- `bun test` passes (new component tests)
- `playwright test e2e/auction-filters.e2e.ts` passes
- Filters work: category, price range, sort
- Pagination works: 25 listings per page
- All components ≤300 LOC, use semantic tokens
- `bin/check` green

## Files Changed
- `apps/web/src/lib/auction.ts` (extend)
- `apps/web/src/queries/useAuction.ts` (modify)
- `apps/web/src/components/FilterBar.tsx` (new)
- `apps/web/src/components/FilterBar.test.tsx` (new)
- `packages/ui/src/Pagination.tsx` (new)
- `packages/ui/src/Pagination.test.tsx` (new)
- `packages/ui/src/index.ts` (export Pagination)
- `apps/web/src/routes/AuctionHouse.tsx` (modify)
- `apps/web/e2e/auction-filters.e2e.ts` (new)
