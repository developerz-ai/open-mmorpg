# PR 051-3: World Feed Filtering, Pagination, Event Types

> **Branch:** `feat/world-feed-polish`
> **Target:** Event type filter, pagination, event type badges

## Tasks

### 1. [coding] Extend feed API with filter/pagination
**File:** `apps/web/src/lib/feed.ts`

- Add `FeedFilters` interface:
  ```typescript
  export interface FeedFilters {
    eventTypes?: string[];  // ['boss_kill', 'world_boss_spawn', ...]
    page?: number;
    perPage?: number;
  }
  ```
- Update `fetchFeed` to accept `FeedFilters`
- Build URL params from filters
- Add `FeedResponse` schema with pagination

### 2. [coding] Update useFeed hook
**File:** `apps/web/src/queries/useFeed.ts`

- Change `useFeed()` to `useFeed(filters: () => FeedFilters)`
- Update queryKey to include filter params
- Keep `refetchInterval: 30_000` for polling

### 3. [coding] Create FeedFilterBar component
**File:** `apps/web/src/components/FeedFilterBar.tsx` (new, ≤100 LOC)

- Event type checkboxes (Combat, Economy, Social, System)
- "Show all" toggle
- All labels via `t()`
- Reuse semantic tokens

### 4. [coding] Update FeedItem component with event badge
**File:** `apps/web/src/components/FeedItem.tsx` (modify)

- Add event type badge (icon + label)
- Map event kind to badge color (combat=red, economy=gold, social=blue, system=gray)
- All badge text via `t('feed.eventType.boss_kill')`

### 5. [coding] Update WorldFeed page
**File:** `apps/web/src/routes/WorldFeed.tsx`

- Add state for filters (create `FeedFilters` signal)
- Add state for pagination (page signal)
- Render `<FeedFilterBar>` above list
- Render `<Pagination>` (from PR 051-2) below list
- Update query to use filters

### 6. [coding] Add feed filter tests
**File:** `apps/web/src/components/FeedFilterBar.test.tsx` (new)

- Test checkbox toggles filter
- Test "Show all" clears filters

### 7. [coding] Add FeedItem badge tests
**File:** `apps/web/src/components/FeedItem.test.tsx` (extend if exists)

- Test event type badge renders
- Test badge color by kind
- Test badge text is i18n'd

### 8. [coding] Add E2E test for feed filters
**File:** `apps/web/e2e/feed-filters.e2e.ts` (new)

- Test: select event type → feed filters
- Test: pagination → navigate feed pages
- Test: "Show all" → shows all event types

## Success Criteria
- `bun test` passes
- `playwright test e2e/feed-filters.e2e.ts` passes
- Event type filter works (checkboxes)
- Pagination works (20 entries per page)
- Event type badges show on feed items
- All components ≤300 LOC
- `bin/check` green

## Files Changed
- `apps/web/src/lib/feed.ts` (extend)
- `apps/web/src/queries/useFeed.ts` (modify)
- `apps/web/src/components/FeedFilterBar.tsx` (new)
- `apps/web/src/components/FeedFilterBar.test.tsx` (new)
- `apps/web/src/components/FeedItem.tsx` (modify)
- `apps/web/src/components/FeedItem.test.tsx` (extend/new)
- `apps/web/src/routes/WorldFeed.tsx` (modify)
- `apps/web/e2e/feed-filters.e2e.ts` (new)
