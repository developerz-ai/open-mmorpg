# PR 051-1: Remaining i18n Locales + Brand Theming Tests

> **Branch:** `feat/web-i18n-complete-locales`
> **Target:** 7 new locale files (ko, ru, pt, it, pl, tr, ar) + brand tests

## Tasks

### 1. [coding] Create 7 missing locale files
**Files to create:** `packages/i18n/src/locales/{ko,ru-pt,it,pl,tr,ar}.ts`

- Reference `en.ts` structure (~219 lines)
- Translate all keys: common, nav, footer, realm, home, errors, auth, armory, auction, feed, downloads, shop
- **Arabic (ar) special handling:**
  - Add RTL layout hints in keys (e.g., `dir: 'rtl'`)
  - Verify mirroring works with CSS logical properties
- Use same TypeScript types from `catalog.ts`

**Validation:**
```bash
bun run --filter @omm/i18n test  # Must pass
```

### 2. [coding] Add locale fallback tests
**File:** `packages/i18n/src/locales.test.ts` (extend existing)

- Add test: missing locale → falls back to `en`
- Add test: unknown locale tag → resolves to `DEFAULT_LOCALE`
- Add test: Arabic locale exports `dir: 'rtl'` metadata

### 3. [coding] Brand theming unit tests
**File:** `apps/web/src/lib/brand.test.ts` (new)

- Test `applyBrand()` sets CSS variables on `:root`
- Test `document.title` updates to realm name
- Test no-op in SSR (typeof document === 'undefined')
- Test accent/accentStrong overrides apply correctly
- Mock `document.documentElement`

### 4. [coding] Add brand visual regression test
**File:** `apps/web/e2e/branding.e2e.ts` (new)

- Test: custom accent color renders in links/buttons
- Test: realm name appears in title
- Use Playwright to verify CSS variable values

### 5. [quick] Update i18n package.json
**File:** `packages/i18n/package.json`

- Verify all 13 locales are exported

## Success Criteria
- `bun test` passes (including new brand tests)
- `bun run --filter @omm/i18n test` passes
- All 13 locales present: en, de, es, fr, ja, zh, **ko, ru, pt, it, pl, tr, ar**
- Arabic includes RTL metadata
- Brand theming has unit + visual regression tests

## Files Changed
- `packages/i18n/src/locales/ko.ts` (new)
- `packages/i18n/src/locales/ru.ts` (new)
- `packages/i18n/src/locales/pt.ts` (new)
- `packages/i18n/src/locales/it.ts` (new)
- `packages/i18n/src/locales/pl.ts` (new)
- `packages/i18n/src/locales/tr.ts` (new)
- `packages/i18n/src/locales/ar.ts` (new)
- `packages/i18n/src/locales.test.ts` (extend)
- `apps/web/src/lib/brand.test.ts` (new)
- `apps/web/e2e/branding.e2e.ts` (new)
