# PR 051-4: Character & Guild Pages Enhancements

> **Branch:** `feat/armory-pages-enhancements`
> **Target:** Achievements, recent activity, guild stats, member roster

## Tasks

### 1. [coding] Extend armory API for achievements/activity
**File:** `apps/web/src/lib/armory.ts`

- Add `CharacterAchievements` schema:
  ```typescript
  export const CharacterAchievementsSchema = z.object({
    total: z.number().int(),
    completed: z.array(z.object({
      id: z.string(),
      name: z.string(),
      completedAt: z.string(),
    })),
  });
  ```
- Add `CharacterActivity` schema (recent kills, quests, etc.)
- Update `fetchCharacter` to include achievements/activity
- Add `GuildRoster` schema for guild members with online status

### 2. [coding] Update useCharacter hook
**File:** `apps/web/src/queries/useArmory.ts`

- Extend to load achievements/activity data
- Keep existing `useCharacter` signature

### 3. [coding] Create AchievementsList component
**File:** `apps/web/src/components/AchievementsList.tsx` (new, ≤150 LOC)

- Show completion percentage (badge)
- List completed achievements with dates
- Show progress bar for incomplete
- All text via `t()`

### 4. [coding] Create ActivityTimeline component
**File:** `apps/web/src/components/ActivityTimeline.tsx` (new, ≤150 LOC)

- Vertical timeline of recent events
- Icon per event type (kill, quest, item)
- Relative timestamps via `fmt.relativeTime()`
- All labels via `t()`

### 5. [coding] Create GuildRoster component
**File:** `apps/web/src/components/GuildRoster.tsx` (new, ≤180 LOC)

- Table of guild members
- Columns: name, rank, level, online status (badge)
- Online status: green "Online" / gray "Offline"
- Sortable by name, level
- All text via `t()`

### 6. [coding] Update CharacterPage
**File:** `apps/web/src/routes/CharacterPage.tsx`

- Add `<AchievementsList>` section
- Add `<ActivityTimeline>` section
- Keep existing gear table
- Update layout for new sections

### 7. [coding] Update GuildPage
**File:** `apps/web/src/routes/GuildPage.tsx`

- Add `<GuildRoster>` section
- Add guild stats (founded date, member count, achievements)
- Keep existing member table (or replace with GuildRoster)

### 8. [coding] Add component tests
**Files:** `apps/web/src/components/{AchievementsList,ActivityTimeline,GuildRoster}.test.tsx` (new)

- Test achievements render correctly
- Test timeline shows events in order
- Test guild roster shows online status
- All text via `t()`

### 9. [coding] Add E2E test for character page
**File:** `apps/web/e2e/character-page.e2e.ts` (new)

- Test: achievements list shows completion %
- Test: activity timeline shows recent events
- Test: gear table still renders

### 10. [coding] Add E2E test for guild page
**File:** `apps/web/e2e/guild-page.e2e.ts` (new)

- Test: guild roster renders members
- Test: online status badge shows
- Test: member count shows correctly

## Success Criteria
- `bun test` passes
- Playwright tests pass
- Character page shows achievements + activity
- Guild page shows roster with online status
- All components ≤300 LOC, use semantic tokens
- `bin/check` green

## Files Changed
- `apps/web/src/lib/armory.ts` (extend)
- `apps/web/src/queries/useArmory.ts` (extend)
- `apps/web/src/components/AchievementsList.tsx` (new)
- `apps/web/src/components/AchievementsList.test.tsx` (new)
- `apps/web/src/components/ActivityTimeline.tsx` (new)
- `apps/web/src/components/ActivityTimeline.test.tsx` (new)
- `apps/web/src/components/GuildRoster.tsx` (new)
- `apps/web/src/components/GuildRoster.test.tsx` (new)
- `apps/web/src/routes/CharacterPage.tsx` (modify)
- `apps/web/src/routes/GuildPage.tsx` (modify)
- `apps/web/e2e/character-page.e2e.ts` (new)
- `apps/web/e2e/guild-page.e2e.ts` (new)
